frontend:
* design product view/list pages
* display product reviews on product page
* display pagination on product list
* escape HTML output (security)

other backend:
* use autoloader
* security (sql injection)
* incapsulate table name and primary key
* incapsulate DB connection object
* problem with connection in controller
* setup phpunit via phpunit.xml
